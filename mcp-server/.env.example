# Gear AI Workspace MCP Server — Environment Variables
# Copy this file to .env and fill in your values.

# ── Supabase ──────────────────────────────────────────────────────────────────
# Your Supabase project URL (e.g., https://xxxxx.supabase.co)
SUPABASE_URL=

# Service role key — grants full DB access (server-side only, never expose to clients)
SUPABASE_SERVICE_KEY=

# ── Server ────────────────────────────────────────────────────────────────────
# Public URL this server will be reachable at (used for MCP metadata)
# Default: http://localhost:3000
MCP_URL=http://localhost:3000

# ── Project ───────────────────────────────────────────────────────────────────
# Absolute path to the gear_ai_v2 project root.
# If omitted, defaults to the parent directory of mcp-server/.
PROJECT_ROOT=
